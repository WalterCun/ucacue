---
export interface Props {
  title?: string;
  subtitle?: string;
  description?: string;
  stats?: Array<{
    number: string;
    label: string;
  }>;
  features?: Array<{
    title: string;
    description: string;
    icon: string;
  }>;
}

const {
  title = "Acerca de Nosotros",
  subtitle = "Más de 10 años transformando ideas en realidades digitales",
  description = "Somos un equipo apasionado de desarrolladores, diseñadores y estrategas digitales comprometidos con la excelencia. Nuestra misión es empoderar a las empresas con soluciones tecnológicas innovadoras que impulsen su crecimiento.",
  stats = [
    { number: "10+", label: "Años de experiencia" },
    { number: "500+", label: "Proyectos exitosos" },
    { number: "50+", label: "Clientes satisfechos" },
    { number: "24/7", label: "Soporte disponible" }
  ],
  features = [
    {
      title: "Innovación Constante",
      description: "Mantenemosnos actualizados con las últimas tecnologías y tendencias del mercado.",
      icon: "🚀"
    },
    {
      title: "Calidad Garantizada",
      description: "Cada proyecto pasa por rigurosos controles de calidad antes de ser entregado.",
      icon: "✨"
    },
    {
      title: "Atención Personalizada",
      description: "Trabajamos de cerca con cada cliente para entender sus necesidades específicas.",
      icon: "🤝"
    },
    {
      title: "Resultados Medibles",
      description: "Nos enfocamos en generar valor tangible y resultados medibles para tu negocio.",
      icon: "📊"
    }
  ]
} = Astro.props;
---

<section id="about" class="section-padding bg-gradient-to-br from-secondary-50 to-white">
  <div class="container-custom">
    <div class="grid lg:grid-cols-2 gap-16 items-center">
      <!-- Content -->
      <div class="space-y-8">
        <div class="space-y-4">
          <h2 class="text-3xl md:text-4xl font-bold text-gray-900">
            {title}
          </h2>
          <p class="text-xl text-primary-600 font-semibold">
            {subtitle}
          </p>
          <p class="text-lg text-gray-600 leading-relaxed">
            {description}
          </p>
        </div>

        <!-- Features -->
        <div class="grid gap-6">
          {features.map((feature) => (
            <div class="flex items-start space-x-4">
              <div class="flex-shrink-0 w-12 h-12 bg-primary-100 rounded-xl flex items-center justify-center text-2xl">
                {feature.icon}
              </div>
              <div>
                <h3 class="text-lg font-semibold text-gray-900 mb-2">
                  {feature.title}
                </h3>
                <p class="text-gray-600">
                  {feature.description}
                </p>
              </div>
            </div>
          ))}
        </div>

        <!-- CTA Button -->
        <div class="pt-4">
          <a href="#contact" class="btn-primary">
            Conoce nuestro equipo
          </a>
        </div>
      </div>

      <!-- Stats & Image -->
      <div class="space-y-8">
        <!-- Stats Grid -->
        <div class="grid grid-cols-2 gap-6">
          {stats.map((stat) => (
            <div class="bg-white rounded-2xl p-6 shadow-lg text-center hover:shadow-xl transition-shadow duration-300">
              <div class="text-3xl font-bold text-primary-600 mb-2">
                {stat.number}
              </div>
              <div class="text-sm text-gray-600 font-medium">
                {stat.label}
              </div>
            </div>
          ))}
        </div>

        <!-- Team Image/Illustration -->
        <div class="relative">
          <div class="bg-gradient-to-br from-primary-400 to-primary-600 rounded-2xl p-8 text-white text-center shadow-2xl">
            <div class="w-24 h-24 bg-white/20 rounded-full mx-auto mb-6 flex items-center justify-center">
              <svg class="w-12 h-12" fill="currentColor" viewBox="0 0 24 24">
                <path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/>
              </svg>
            </div>
            <h3 class="text-xl font-bold mb-2">Equipo Experto</h3>
            <p class="text-primary-100">
              Profesionales certificados en las últimas tecnologías
            </p>
          </div>

          <!-- Decorative elements -->
          <div class="absolute -top-4 -right-4 w-20 h-20 bg-yellow-400 rounded-full opacity-20 animate-pulse"></div>
          <div class="absolute -bottom-4 -left-4 w-16 h-16 bg-pink-400 rounded-full opacity-20 animate-pulse delay-1000"></div>
        </div>
      </div>
    </div>
  </div>
</section>
