---
export interface Props {
  title?: string;
  subtitle?: string;
  services?: Array<{
    title: string;
    description: string;
    icon: string;
    color: string;
  }>;
}

const {
  title = "Nuestros Servicios",
  subtitle = "Soluciones integrales para tu negocio digital",
  services = [
    {
      title: "Desarrollo Web",
      description: "Sitios web modernos, responsivos y optimizados para SEO que convierten visitantes en clientes.",
      icon: "🌐",
      color: "from-blue-500 to-blue-600"
    },
    {
      title: "Aplicaciones Móviles",
      description: "Apps nativas e híbridas para iOS y Android que mantienen a tus usuarios conectados.",
      icon: "📱",
      color: "from-green-500 to-green-600"
    },
    {
      title: "E-commerce",
      description: "Tiendas online completas con pasarelas de pago seguras y gestión de inventario.",
      icon: "🛒",
      color: "from-purple-500 to-purple-600"
    },
    {
      title: "Marketing Digital",
      description: "Estrategias de SEO, SEM y redes sociales para aumentar tu presencia online.",
      icon: "📈",
      color: "from-orange-500 to-orange-600"
    },
    {
      title: "Consultoría IT",
      description: "Asesoramiento experto para optimizar tu infraestructura tecnológica y procesos.",
      icon: "💡",
      color: "from-red-500 to-red-600"
    },
    {
      title: "Mantenimiento",
      description: "Soporte técnico continuo y actualizaciones para mantener tu sistema funcionando perfectamente.",
      icon: "🔧",
      color: "from-indigo-500 to-indigo-600"
    }
  ]
} = Astro.props;
---

<section id="services" class="section-padding bg-white">
  <div class="container-custom">
    <!-- Header -->
    <div class="text-center mb-16">
      <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">
        {title}
      </h2>
      <p class="text-xl text-gray-600 max-w-3xl mx-auto">
        {subtitle}
      </p>
    </div>

    <!-- Services Grid -->
    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
      {services.map((service, index) => (
        <div 
          class="group bg-white rounded-2xl p-8 shadow-lg hover:shadow-2xl transition-all duration-300 transform hover:-translate-y-2 border border-gray-100"
          style={`animation-delay: ${index * 100}ms`}
        >
          <!-- Icon -->
          <div class={`w-16 h-16 rounded-2xl bg-gradient-to-r ${service.color} flex items-center justify-center text-3xl mb-6 group-hover:scale-110 transition-transform duration-300`}>
            {service.icon}
          </div>

          <!-- Content -->
          <h3 class="text-xl font-bold text-gray-900 mb-4 group-hover:text-primary-600 transition-colors">
            {service.title}
          </h3>
          
          <p class="text-gray-600 leading-relaxed mb-6">
            {service.description}
          </p>

          <!-- Learn More Link -->
          <a href="#" class="inline-flex items-center text-primary-600 font-medium hover:text-primary-700 transition-colors group-hover:translate-x-1 transition-transform">
            Saber más
            <svg class="w-4 h-4 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
            </svg>
          </a>
        </div>
      ))}
    </div>

    <!-- CTA Section -->
    <div class="text-center mt-16">
      <div class="bg-gradient-to-r from-primary-50 to-secondary-50 rounded-2xl p-12">
        <h3 class="text-2xl font-bold text-gray-900 mb-4">
          ¿Necesitas algo personalizado?
        </h3>
        <p class="text-gray-600 mb-8 max-w-2xl mx-auto">
          Cada proyecto es único. Trabajamos contigo para crear soluciones a medida que se adapten perfectamente a tus necesidades específicas.
        </p>
        <a href="#contact" class="btn-primary">
          Hablar con un experto
        </a>
      </div>
    </div>
  </div>
</section>
